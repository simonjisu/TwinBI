Run the following command to set up the TPC-DS database for use with Agent4OLAP:

```bash
$ cd cube-project
$ uv run ./create_tutorial_data.py
```

Copy database to this folder from `Agent4OLAP/cube-project/data/tutorial`. 