cubes:
  - name: income_band
    sql_table: main.income_band
    data_source: default

    joins: []

    dimensions:
      - name: ib_income_band_sk
        sql: ib_income_band_sk
        type: number
        primary_key: true

      - name: ib_lower_bound
        sql: ib_lower_bound
        type: number

      - name: ib_upper_bound
        sql: ib_upper_bound
        type: number

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started
